const { $ } = require('@wdio/globals')
const Page = require('./launch');

/**
 * sub page containing specific selectors and methods for a specific page
 */
class microwavePage extends Page {
    /**
     * define selectors using getter methods
     */

}